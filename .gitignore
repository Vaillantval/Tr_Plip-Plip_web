__pycache__/
*.py[cod]
.env
dev.sqlite3
staticfiles/
.venv/
.pytest_cache/
.claude/settings.local.json

# CSS de la console : genere par Tailwind (npm run build:css), compile
# dans l'image par le Dockerfile. Ne pas committer.
node_modules/
apps/console/static/console/tailwind.css

# Sauvegardes. Un export contient les numeros de telephone et les montants
# de TOUS les clients : il ne part jamais dans git, meme sur un depot prive.
sauvegardes/
plipplip-*.json
plipplip-*.json.enc
